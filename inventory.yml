plugin: aws_ec2
  #credentials:
  #aws_access_key: "{{ aws_access_key }}"
  #aws_secret_key: "{{ aws_secret_key }}"
aws_access_key: 
aws_secret_key: 
regions:
  - ap-south-1
filters:
  instance-state-name: running
hostnames:
  - 172.31.14.134
compose:
  ansible_host: 172.31.9.212
